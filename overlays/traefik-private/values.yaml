image:
  tag: "3.6.13"

deployment:
  name: traefik-private
  kind: Deployment
  replicas: 1
  strategy:
    type: Recreate

service:
  enabled: true
  type: LoadBalancer
  externalTrafficPolicy: Local
  annotations:
      metallb.universe.tf/address-pool: "tailscale-ip-pool"

ports:
  web:
    expose:
      enabled: false
  websecure:
    port: 443          # This is the port on the LoadBalancer Service
    exposedPort: 443   # Expose 443 publicly
    expose:
      enabled: true

api:
  dashboard: true
  insecure: false

providers:
  kubernetesCRD:
    enabled: true
    allowCrossNamespace: true
  kubernetesIngress:
    ingressClass: traefik-private
    ingressEndpoint:
      publishedService: traefik-private/traefik-private

ingressClass:
  enabled: true
  isDefaultClass: false
  name: traefik-private
  controller: traefik.io/private

log:
  general:
     level: DEBUG
  access: true


ingressRoute:
  dashboard:
    enabled: true
    domain: callandor.tailcdecb.ts.net
    matchRule: Host(`callandor.tailcdecb.ts.net`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))
    entryPoints:
      - websecure
    rules:
      - host: callandor.tailcdecb.ts.net
        http:
          paths:
            - path: /dashboard
              pathType: Prefix
  tls:
    secretName: callandor-tailscale-tls
